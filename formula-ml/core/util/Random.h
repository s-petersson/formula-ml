#ifndef CORE_UTIL_RANDOM
#define CORE_UTIL_RANDOM

/** Generates a random float between 0 and 1. */
float rngf();

/** Generates a random float between min and max*/
float rngf(float min, float max);

#endif